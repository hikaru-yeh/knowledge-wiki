# Raw Source Inbox

`raw/` is the private source inbox for saved posts, exports, documents, and other source material.

In the public scaffold, real raw files are intentionally omitted. Treat this directory as read-only evidence in a private working vault.

Use `raw/examples/` for small synthetic examples that are safe to publish.
